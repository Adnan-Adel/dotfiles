return {
    {
        "echasnovski/mini.ai",
        event = "VeryLazy",
        config = function()
            require("mini.ai").setup({
                n_lines = 500,
            })
        end,
    },
    --[[
        -- dif ====> Delete in function
        -- daf ====> Delete arounf function
        -- daa ====> Delete around argument
        -- via ====> Select in argument
        -- viq ====> Select in quote (any type!)
        -- vif ====> Select in function
    --]]
}
