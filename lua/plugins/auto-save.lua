return {
  -- "pocco81/auto-save.nvim",
  -- config = function()
    -- require("auto-save").setup({
      -- enabled = false,
      -- trigger_events = { "InsertLeave", "TextChanged" },
      -- debounce_delay = 1000,
    -- })
  -- end,
}
